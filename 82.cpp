#include <bits/stdc++.h>
using namespace std;
//Strongly connected components are the ones if you can reach from one vertex to another.
//Collection of connected component is a forest
//Children of a node(if we catch that node and hanging nodes are child)
//Parent of a node
//Depth -distance from the node to root node
//Height -distance of a node from the leaf node(longest distance -maximum edges)
//Lowest common Ancestor-If we go from a two nodes to top,the common parent node is LCA
int main(){

}
