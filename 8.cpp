#include <bits/stdc++.h>
using namespace std;
const int n=1e7;//whose value never change
int a[n];//globally for array more than 10^5 is accepted ,upto 10^7 
int main()
{
    // int a[10]; // if you try to access a[10] or greater index it will give segmentation fault(as for this memory is not allocated for it)
    // a[11]=3 may work or may not as it is an undefined behaviour,so it is not advised to do it.
    // int n;
    // cin >> n;
    // int a[n]; // one can declare array of any datatype ex: char[10]
    // a[4] is a collection of 4 a, a[4][3] is a collection of 3 a[4]
    //  for (int i=0; i<n; ++i)
    //  {
    //      cin >> a[i];
    //  }
    //  int sum = 0;
    //  for (int i = 0; i < n; ++i)
    //  {
    //      sum = sum + a[i];
    //  }
    //  cout << sum;
    // int n, m;
    // cin >> n >> m;
    // int a[n][m];
    // for (int i = 0; i < n; ++i)
    // {
    //     for (int j = 0; j < m; ++j)
    //     {
    //         cin >> a[i][j];
    //     }
    // }
    // for (int i = 0; i < n; ++i)
    // {
    //     for (int j = 0; j < m; j++)
    //     {
    //         cout << a[i][j] << " ";
    //     }
    //     cout << endl;
    // }
    //array sizes can be upto 10^5 locally
    // int n=1e5;
    // int a[n];
    // a[n-1]=7;
    // cout<<a[n-1];
}